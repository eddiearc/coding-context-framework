# Task Domains

任务域保存仓库映射、默认 base branch、业务专属验收边界和证据入口。顶层规则保持通用，具体规则进入 `docs/domains/<domain>.md`。

当前只有 `general.md` 作为空白占位。引入真实工作流时，优先从 `_template.md` 新增独立 domain 文件，再在 task board 中登记 domain 和 repo。
